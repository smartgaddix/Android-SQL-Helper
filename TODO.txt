Roadmap:

X define annotations
X define model
X implement the annotation processor via apt (integrated into javac.exe)
X loading schema properties from properties file
X use codemodel for code generation
X make test case
X make same sample
X javadoc
X use of entity annotation to force some field properties, like key or nullable
X update/remove method based on key fields (without row id)
X support of date time fields. Support of standard conversion or use a custom converter class
X support for license inclusion at the beginning of generated files(from external file)
- support for custom class as class member
- support for field of type enum
- support for index creation

Todo:

X optimize test project's settings for path dependency
X optimize test project's sources
X table annotation value for field name prefix to ignore for column name
X store field type for conversion in code generation
X try to use codemodel 2.5
X plan the use of the needed library codemodel and android.jar
X make a sample stand alone project
X make a sample android project
X javadoc
X README for sample project and tests
X complete the README with:
		X add used libraries for license reasons
		X supported types and sql mapping
		X use of the tool
X clean project for tag
X search for TODO
X tag
